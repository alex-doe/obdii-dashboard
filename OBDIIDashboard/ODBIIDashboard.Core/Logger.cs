namespace ODBIIDashboard.Core {
    public class Logger {        

        public Logger() {
            //_logview = main.lstLoggGrid;
        }

        public void WriteLine(string text) {
            //_logview.Items.Add(text);
        }
    }
}